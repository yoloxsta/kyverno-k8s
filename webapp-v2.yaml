apiVersion: v1
kind: Pod
metadata:
  name: webapp
spec:
  securityContext:
    runAsNonRoot: true
  containers:
  - name: webapp-container
    image: kodekloud/webapp-color:v1
    securityContext:
      runAsUser: 1000  # Non-root user